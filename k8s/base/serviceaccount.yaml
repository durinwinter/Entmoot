# Entmoot never calls the Kubernetes API, so this carries no RBAC bindings —
# it exists so the pods don't run under `default` (good hygiene, and a
# prerequisite for some cluster admission policies).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: entmoot
  labels:
    app: entmoot
